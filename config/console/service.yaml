apiVersion: v1
kind: Service
metadata:
  name: agent-console
  labels:
    app.kubernetes.io/name: agent-console
spec:
  selector:
    app.kubernetes.io/name: agent-console
  ports:
    - name: oauth
      port: 4180
      targetPort: oauth
    - name: http
      port: 8080
      targetPort: http
